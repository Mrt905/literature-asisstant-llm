apiVersion: 1

datasources:
  - name: PostgreSQL
    type: postgres
    url: postgres:5432
    user: user
    secureJsonData:
      password: pswd
    jsonData:
      database: monitoring
      sslmode: disable
      postgresVersion: 1700
    isDefault: true